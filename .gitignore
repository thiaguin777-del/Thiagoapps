# Ambiente de teste visual.
# Versionados: só os scripts do harness na raiz de .devtest/.
# Ignorado: TODA subpasta — node_modules (reproduzível com
# `npm i three@0.160.0`) e as saídas de render do shoot.mjs, que ganham
# nome novo a cada rodada.
#
# Cuidado: a versão anterior deste arquivo tinha `!.devtest/node_modules/`,
# uma negação que reincluía justamente o que a linha acima excluía — e
# 976 arquivos de dependência entraram na árvore por isso.
.devtest/*/
.devtest/package-lock.json
.devtest/index.html
